import { Routes } from '@angular/router';
import { CategoriesComponent } from './components/card/categories.component';

export const routes: Routes = [
  { path: '', component: CategoriesComponent, title: 'Recipes' },
];
